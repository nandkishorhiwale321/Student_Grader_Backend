spring.application.name=StudentGrader

# Hibernate
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.defer-datasource-initialization=true
#spring.sql.init.mode=always

# Database (use Railway env vars)
#spring.datasource.url=jdbc:mysql://localhost:3306/grader
#spring.datasource.username=root
#spring.datasource.password=root

# Hot reload (local only, won't matter in Railway)
#spring.devtools.restart.enabled=true 

# ===============================
## ===============================
# DATABASE CONFIGURATION (Railway MySQL)
# ===============================
spring.application.name=StudentGrader

# ===============================
# DATABASE CONFIGURATION (Railway)
# ===============================
spring.datasource.url=jdbc:mysql://turntable.proxy.rlwy.net:42713/railway
spring.datasource.username=root
spring.datasource.password=GhScxuAOFSMyvUypcKtEmQjJQWWFvsgS
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===============================
# SQL INIT
# ===============================
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# ===============================
# MAIL CONFIG (USE ENV VARIABLES)
# ===============================
sendgrid.api.key=${SENDGRID_API_KEY}
sendgrid.from.email=nandkishorhiwale321@gmail.com